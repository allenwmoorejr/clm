export function nextServiceDate(){ const now=new Date(); const d=new Date(now); const day=d.getDay(); const daysToSunday=(7-day)%7; d.setDate(d.getDate()+((daysToSunday===0 && (d.getHours()>=10))?7:daysToSunday)); d.setHours(10,0,0,0); return d;} export function countdownParts(t:Date){ const diff=Math.max(0,t.getTime()-Date.now()); const days=Math.floor(diff/86400000); const hours=Math.floor((diff%86400000)/3600000); const minutes=Math.floor((diff%3600000)/60000); return {days,hours,minutes}; }